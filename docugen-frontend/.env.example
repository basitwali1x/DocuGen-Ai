# API URL for the DocuGen backend (without embedded credentials)
VITE_API_URL=https://your-api-domain.com

# API key for backend authentication
VITE_API_KEY=your_api_key_here

# Proxy target for development (with authentication if needed)
VITE_PROXY_TARGET=https://your-backend-tunnel-url.com
